package day39_a_polymorphism;

public class CastingInAutomation {
    /*
        WebDriver driver =new ChromeDriver();  // We want to limit the access
        ChromeDriver driver2 = (ChromeDriver)driver;

        driver2.takeScreenShot();
 */
}
